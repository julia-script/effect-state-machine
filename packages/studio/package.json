{
  "name": "@effect-state-machine/studio",
  "version": "0.1.0",
  "description": "Studio \u2014 the standalone devtool for effect-state-machine.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "effect-state-machine-studio": "./bin/studio.mjs"
  },
  "files": [
    "bin",
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && esbuild src/cli.ts --bundle --platform=node --format=esm --target=node22 --outfile=dist/server/cli.js --external:effect --external:@effect/platform-node",
    "check": "pnpm typecheck && pnpm test",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@effect-state-machine/studio-client": "workspace:*",
    "@effect/platform-node": "4.0.0-beta.106",
    "effect": "4.0.0-beta.106",
    "effect-state-machine": "workspace:*"
  },
  "devDependencies": {
    "@effect/vitest": "4.0.0-beta.106",
    "esbuild": "0.28.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
