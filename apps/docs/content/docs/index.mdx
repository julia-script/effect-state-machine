---
title: effect-state-machine
description: Code-first, Effect-native state machines for explicit application behavior.
---

`effect-state-machine` is a code-first state-machine library for TypeScript applications built
with Effect. A machine definition describes valid inputs, states, events, transitions, invoked
Effects, timers, regions, and child machines as one immutable value. The same value can be run by the interpreter
or projected into a read-only graph for development tooling.

The library is intended for Effect users who want orchestration to remain visible in ordinary
TypeScript. It assumes familiarity with `Effect`, `Schema`, `Layer`, and scoped resources.

## Start here

Follow [Build your first machine](/docs/tutorials/first-machine) to create and run a small machine
from an empty project. The tutorial covers the core loop: describe machine data with Schema,
construct nodes, start a scoped machine instance, send events, and observe completion.

To see the browser devtools in action, open
[Explore an embedded Studio](/docs/tutorials/embedded-studio). It includes a live machine that you
can inspect and dispatch from directly on the documentation page.

When you already know the result you need, use the how-to guides:

- [Run an Effect when a state becomes active](/docs/guides/invoke-effects)
- [Model parallel regions](/docs/guides/model-parallel-regions)
- [Choose transitions with guards](/docs/guides/guards-and-ignored-events)
- [Retry invoked work](/docs/guides/retry-invoked-work)
- [Compose behavior with a child machine](/docs/guides/child-machines)
- [Observe a running machine](/docs/guides/observe-a-machine)
- [Visualize a machine definition](/docs/guides/visualize-a-definition)

For exact signatures and runtime constraints, use the reference:

- [Machine definition API](/docs/reference/machine-definition)
- [Machine runtime API](/docs/reference/machine-runtime)
- [Graph, Mermaid, and source-location API](/docs/reference/devtools)

For the design model and its trade-offs, read
[Why the library is Effect-native](/docs/explanation/effect-native-design).

## Installation

The current v0 release targets the Effect beta used to verify its semantics:

```sh
pnpm add effect-state-machine effect
```

The package is ESM, targets ES2022, and exposes the main API from `effect-state-machine`. Static
graph tooling is also available from the opt-in `effect-state-machine/devtools` entry point.

## What v0 models

A machine node has one visible behavior kind:

- an ordinary state with pure transitions and an optional entry-owned timer;
- invoked work with typed success and failure routes, including named `all` and `race` lanes;
- a region-bearing state with one compound or several parallel tagged-union slots;
- an invoked child machine with explicit event forwarding;
- a final state whose value becomes the machine completion value.

Machine instances serialize external events and asynchronous completions through one queue. Work
owned by an invoked or child state is interrupted when the machine leaves that state.

V0 does not provide nested parent-state hierarchy, dynamic actors, visual editing, durable
execution, persistence, replay, or application-framework bindings beyond the React Studio.
