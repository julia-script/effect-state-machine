---
title: MachineWorkflow API
description: Generated reference for the public MachineWorkflow module.
full: true
---

{/* Generated by `pnpm docs:api`. Do not edit by hand. */}

[**effect-state-machine API**](/docs/reference/api)

***

[effect-state-machine API](/docs/reference/api) / MachineWorkflow

# MachineWorkflow

## configuration

### InvokeConfig

Defined in: [packages/core/src/MachineWorkflow.ts:72](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L72)

Configuration for one Workflow-backed machine invocation.

**Details**

`payload` receives the narrowed machine state and required stable work execution. The Workflow's
declared success and failure Schemas determine the corresponding machine transition reducers.

#### Since

0.2.0

#### Type Parameters

##### State

`State` *extends* [`Tagged`](/docs/reference/api/Machine#tagged)

##### Current

`Current` *extends* [`TagOf`](/docs/reference/api/Machine#tagof)\<`State`\>

##### Definition

`Definition` *extends* `AnyWorkflow`

#### Properties

##### workflow

> `readonly` **workflow**: `Definition`

Defined in: [packages/core/src/MachineWorkflow.ts:77](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L77)

##### name?

> `readonly` `optional` **name?**: `string`

Defined in: [packages/core/src/MachineWorkflow.ts:78](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L78)

##### description?

> `readonly` `optional` **description?**: `string`

Defined in: [packages/core/src/MachineWorkflow.ts:79](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L79)

##### payload

> `readonly` **payload**: (`args`) => `Type`\<`PayloadSchema`\<`NoInfer`\<...\>\>\>

Defined in: [packages/core/src/MachineWorkflow.ts:80](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L80)

###### Parameters

###### args

`Readonly`\<\{ `state`: [`ByTag`](/docs/reference/api/Machine#bytag)\<..., ...\>; `execution`: [`WorkExecution`](/docs/reference/api/Machine#workexecution); \}\>

###### Returns

`Type`\<`PayloadSchema`\<`NoInfer`\<...\>\>\>

##### onSuccess

> `readonly` **onSuccess**: [`SuccessTransition`](/docs/reference/api/Machine#successtransition)\<`State`, `Current`, `Type`\<`SuccessSchema`\<`NoInfer`\<...\>\>\>\>

Defined in: [packages/core/src/MachineWorkflow.ts:86](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L86)

##### onFailure

> `readonly` **onFailure**: [`FailureTransition`](/docs/reference/api/Machine#failuretransition)\<`State`, `Current`, `Type`\<`FailureSchema`\<`NoInfer`\<...\>\>\>\>

Defined in: [packages/core/src/MachineWorkflow.ts:87](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L87)

## constructors

### executionId()

> **executionId**(`workflowName`, `machineExecutionId`): `string`

Defined in: [packages/core/src/MachineWorkflow.ts:56](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L56)

Derives the Workflow execution identity owned by one machine work execution.

**Details**

The versioned, length-prefixed encoding separates Workflow names and machine execution IDs
without relying on a delimiter that user input could collide with. Redelivery of the same
machine work produces the same Workflow execution ID.

#### Parameters

##### workflowName

`string`

##### machineExecutionId

[`ExecutionId`](/docs/reference/api/MachineStore#executionid)

#### Returns

`string`

#### Since

0.2.0

***

### invoke()

> **invoke**\<`InputSchema`, `StateSource`, `EventSource`, `Definition`, `Current`\>(`builder`, `config`): `Readonly`\<\{ `description?`: ... \| ...; `source`: `Reference`; `invoke`: [`InvokeSpecBase`](/docs/reference/api/Machine#invokespecbase); `on?`: ... \| ...; `after?`: ... \| ...; `regions?`: `undefined`; `child?`: `undefined`; `onComplete?`: `undefined`; `final?`: `undefined`; \}\> & `Readonly`\<\{ `invoke`: `EffectInvokeSpec`\<..., ..., ..., ..., ..., ..., ..., ...\>; \}\>

Defined in: [packages/core/src/MachineWorkflow.ts:112](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L112)

Declares Workflow-backed work using the same activity-outcome seam as an ordinary invocation.

Redelivery reuses a versioned execution ID derived from the Workflow name and stable machine
execution ID. The Workflow engine therefore resumes or observes the same execution while the
machine still persists the decoded outcome through its own aggregate protocol.

**When to use**

Use when invoked machine work should be executed and resumed by Effect Workflow while machine
state remains owned by `MachineEngine` and `MachineStore`.

**Gotchas**

Workflow persistence does not replace the machine store. The resulting definition requires both
the Workflow engine and any client services required by the Workflow, in addition to the machine
engine required when the definition runs.

#### Type Parameters

##### InputSchema

`InputSchema` *extends* `Top`

##### StateSource

`StateSource` *extends* [`TaggedSchemaSource`](/docs/reference/api/Machine#taggedschemasource)

##### EventSource

`EventSource` *extends* [`TaggedSchemaSource`](/docs/reference/api/Machine#taggedschemasource)

##### Definition

`Definition` *extends* `Any`

##### Current

`Current` *extends* `string` = [`TagOf`](/docs/reference/api/Machine#tagof)\<`Type`\<[`NormalizedTaggedSchema`](/docs/reference/api/Machine#normalizedtaggedschema)\<`StateSource`\>\>\>

#### Parameters

##### builder

###### child

\<`Current`, `Child`\>(`config`, `on?`) => `ChildNodeConfig`\<`State`, `Event`, `Current`, `Child`\>

###### define

\<`States`\>(`config`, `states`) => [`MachineDefinition`](/docs/reference/api/Machine#machinedefinition)\<`InputSchema`, [`NormalizedTaggedSchema`](/docs/reference/api/Machine#normalizedtaggedschema)\<...\>, [`NormalizedTaggedSchema`](/docs/reference/api/Machine#normalizedtaggedschema)\<...\>, `States`\>

###### final

(`options?`) => `FinalNodeConfig`

###### guard

\<`Args`\>(`config`) => [`NamedGuard`](/docs/reference/api/Machine#namedguard)\<`Args`\> = `namedGuard`

###### invoke

\<`Current`, `Success`, `AllowedFailure`, `Failure`, `Requirements`, `RetryError`, `RetryEnv`\>(`config`, `on?`, `options?`) => ... & ... & `object`

###### region

\{ `invoke`: \<`Current`, `Slot`, `RegionTag`, `Success`, `AllowedFailure`, `Failure`, `Requirements`, `RetryError`, `RetryEnv`\>(`config`, `on?`, `options?`) => `Readonly`\<...\>; \} = `...`

###### region.invoke

\<`Current`, `Slot`, `RegionTag`, `Success`, `AllowedFailure`, `Failure`, `Requirements`, `RetryError`, `RetryEnv`\>(`config`, `on?`, `options?`) => `Readonly`\<...\> = `invokeRegion`

###### regions

\<`Current`, `RegionTrees`\>(`regionTrees`, `on?`, `options?`) => `Readonly`\<...\> & `Readonly`\<...\>

###### state

\<`Current`\>(`on`, `options?`) => `AtomicNodeConfig`\<`State`, `Event`, `Current`\>

###### schemas

`Readonly`\<\{ `input`: `InputSchema`; `state`: [`NormalizedTaggedSchema`](/docs/reference/api/Machine#normalizedtaggedschema); `event`: [`NormalizedTaggedSchema`](/docs/reference/api/Machine#normalizedtaggedschema); \}\>

##### config

[`InvokeConfig`](#invokeconfig)\<`Type`\<[`NormalizedTaggedSchema`](/docs/reference/api/Machine#normalizedtaggedschema)\<`StateSource`\>\>, `Current`, `Definition`\>

#### Returns

`Readonly`\<\{ `description?`: ... \| ...; `source`: `Reference`; `invoke`: [`InvokeSpecBase`](/docs/reference/api/Machine#invokespecbase); `on?`: ... \| ...; `after?`: ... \| ...; `regions?`: `undefined`; `child?`: `undefined`; `onComplete?`: `undefined`; `final?`: `undefined`; \}\> & `Readonly`\<\{ `invoke`: `EffectInvokeSpec`\<..., ..., ..., ..., ..., ..., ..., ...\>; \}\>

#### See

[executionId](#executionid) for the stable identity used on redelivery.

#### Since

0.2.0

## utility types

### Payload

> **Payload**\<`Definition`\> = `Schema.Schema.Type`\<`PayloadSchema`\<`Definition`\>\>

Defined in: [packages/core/src/MachineWorkflow.ts:24](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L24)

Decoded payload accepted by a Workflow definition.

#### Type Parameters

##### Definition

`Definition` *extends* `AnyWorkflow`

#### Since

0.2.0

***

### Success

> **Success**\<`Definition`\> = `Schema.Schema.Type`\<`SuccessSchema`\<`Definition`\>\>

Defined in: [packages/core/src/MachineWorkflow.ts:32](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L32)

Decoded success produced by a Workflow definition.

#### Type Parameters

##### Definition

`Definition` *extends* `AnyWorkflow`

#### Since

0.2.0

***

### Failure

> **Failure**\<`Definition`\> = `Schema.Schema.Type`\<`FailureSchema`\<`Definition`\>\>

Defined in: [packages/core/src/MachineWorkflow.ts:40](https://github.com/julia-script/effect-state-machine/blob/main/packages/core/src/MachineWorkflow.ts#L40)

Declared error produced by a Workflow definition.

#### Type Parameters

##### Definition

`Definition` *extends* `AnyWorkflow`

#### Since

0.2.0
